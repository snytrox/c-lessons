#include <stdio.h>
#include <stdlib.h>

int main() {
int i=1,num1=0;
printf("enter a number from 1 to 10:");
scanf("%d",&num1);
if(num1<0 || num1>10){
    printf("please dont do that\n");
    return 0;
}
  while(i<=10){
    printf("%d x %d = %d\n",num1,i,num1*i);
    i++;
  }
 return 0;
}
